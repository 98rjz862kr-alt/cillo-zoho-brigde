name: Boa Bridge HTTP status diagnostic

on:
  push:
    branches:
      - agent/boa-totem-soya-render-verify-20260816
    paths:
      - .github/workflows/boa-bridge-http-status.yml

permissions:
  contents: read

jobs:
  status:
    runs-on: ubuntu-latest
    steps:
      - name: Lire les endpoints Render et domaine Bridge
        run: |
          set -x
          echo '--- custom domain ---'
          curl --fail --silent --show-error --max-time 30 https://bridge.lesmotsimages.com/health
          echo
          echo '--- render domain ---'
          curl --fail --silent --show-error --max-time 30 https://cillo-zoho-bridge.onrender.com/health
          echo
# probe 2026-08-16T14:55+02:00
