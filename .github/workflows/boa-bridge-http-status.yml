name: Boa Bridge HTTP status diagnostic

on:
  push:
    branches:
      - agent/boa-totem-soya-render-verify-20260816
    paths:
      - .github/workflows/boa-bridge-http-status.yml

permissions:
  contents: read

jobs:
  status:
    runs-on: ubuntu-latest
    steps:
      - name: Comparer les trois endpoints du Bridge
        run: |
          set -x
          echo '--- custom ---'
          curl --fail --silent --show-error --max-time 30 https://bridge.lesmotsimages.com/health; echo
          echo '--- custom origin Render ---'
          curl --fail --silent --show-error --max-time 30 https://cillo-zoho-bridge-2.onrender.com/health; echo
          echo '--- blueprint name Render ---'
          curl --fail --silent --show-error --max-time 30 https://cillo-zoho-bridge.onrender.com/health; echo
# probe 2026-08-16T15:00+02:00
