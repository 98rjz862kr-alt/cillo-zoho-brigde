name: LMI FOOD quality

on:
  push:
    branches: [main]
    paths:
      - "drafts/lmi-food-site/**"
      - "scripts/validate-lmi-food.mjs"
      - "package.json"
      - ".github/workflows/lmi-food-quality.yml"
  pull_request:
    paths:
      - "drafts/lmi-food-site/**"
      - "scripts/validate-lmi-food.mjs"
      - "package.json"
      - ".github/workflows/lmi-food-quality.yml"
  workflow_dispatch:

permissions:
  contents: read

jobs:
  validate-lmi-food:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
      - name: Run repository validation
        run: npm test
      - name: Validate LMI FOOD only
        run: node scripts/validate-lmi-food.mjs
